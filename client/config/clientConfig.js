Meta.config({
    options: {
        // Meteor.settings[Meteor.settings.environment].public.meta.title
        title: 'GroupInsurance App',
        suffix: 'GroupInsurance App'
    }
});

Accounts.ui.config({
    passwordSignupFields: 'USERNAME_AND_EMAIL'
});